Read the task file provided for this session.

Write failing tests for the acceptance criteria listed in that file.
Follow the testing patterns already present in this codebase.

Rules:
- Tests MUST fail — the feature does not exist yet
- No implementation code
- Each acceptance criterion gets at least one test
- Descriptive test names stating expected behavior

Run the test suite: confirm new tests fail, all existing tests pass.
Commit the tests with message referencing the spec.
